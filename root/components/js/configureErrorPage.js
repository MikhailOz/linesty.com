function titlePage(title) {
    titleDOM = document.getElementById("error-title");
    titleDOM.innerText = title;
}

function subtitlePage(subtitle) {
    subtitleDOM = document.getElementById("error-subtitle");
    subtitleDOM.innerText = subtitle;
}